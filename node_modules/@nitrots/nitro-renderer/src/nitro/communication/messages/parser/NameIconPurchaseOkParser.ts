import { IMessageDataWrapper, IMessageParser } from '@nitrots/nitro-renderer';

export class NameIconPurchaseOkParser implements IMessageParser {
    private _iconId: number = 0;
    private _diamonds: number = 0;

    public flush(): boolean {
        this._iconId = 0;
        this._diamonds = 0;
        return true;
    }

    public parse(wrapper: IMessageDataWrapper): boolean {
        if (!wrapper) return false;

        this._iconId = wrapper.readInt();
        this._diamonds = wrapper.readInt();
        return true;
    }

    public get iconId(): number {
        return this._iconId;
    }
    public get diamonds(): number {
        return this._diamonds;
    }
}
