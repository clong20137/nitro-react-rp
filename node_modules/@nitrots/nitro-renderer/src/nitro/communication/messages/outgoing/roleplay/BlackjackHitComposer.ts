import { IMessageComposer } from '@nitrots/nitro-renderer';

/** If you later support multiple hands, add a handIndex param here. */
export class BlackjackHitComposer implements IMessageComposer<number[]> {
    getMessageArray(): number[] {
        return [];
    }
    dispose(): void {
        /* no-op */
    }
}
