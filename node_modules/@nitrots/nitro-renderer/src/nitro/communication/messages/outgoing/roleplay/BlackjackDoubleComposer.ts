import { IMessageComposer } from '@nitrots/nitro-renderer';

export class BlackjackDoubleComposer implements IMessageComposer<number[]> {
    getMessageArray(): number[] {
        return [];
    }
    dispose(): void {
        /* no-op */
    }
}
