import { IMessageComposer } from '@nitrots/nitro-renderer';

/**
 * Sends a request to spin the slot machine reels.
 * Includes bet amount (credits) and optional tableId for multi-machine support.
 */
export class SlotSpinComposer implements IMessageComposer<number[]> {
    private _data: number[];

    constructor(betAmount: number, tableId: number = 0) {
        // Enforce bet limits: 5–25 credits
        const bet = Math.max(5, Math.min(25, betAmount));
        this._data = [tableId, bet];
        console.log('spin sent!');
    }

    public getMessageArray(): number[] {
        return this._data;
    }

    public dispose(): void {
        // no-op
    }
}
