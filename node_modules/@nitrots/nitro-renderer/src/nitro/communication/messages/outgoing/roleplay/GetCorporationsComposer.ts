import { IMessageComposer } from '@nitrots/nitro-renderer';

export class GetCorporationsComposer implements IMessageComposer<any[]> {
    private _data: any[];

    constructor() {
        this._data = []; // No arguments needed — just triggers a fetch
    }

    public getMessageArray(): any[] {
        console.log('Sending GetCorporationsComposer');
        return this._data;
    }

    public dispose(): void {}
}
