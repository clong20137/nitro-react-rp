import { IMessageComposer } from '@nitrots/nitro-renderer';

export class JukeboxTrackEndedComposer implements IMessageComposer<any[]> {
    private _data: any[];

    constructor() {
        // no params needed
        this._data = [];

        console.log('[📤] JukeboxTrackEndedComposer packet sent');
    }

    public getMessageArray(): any[] {
        return this._data;
    }

    public dispose(): void {
        return;
    }
}
