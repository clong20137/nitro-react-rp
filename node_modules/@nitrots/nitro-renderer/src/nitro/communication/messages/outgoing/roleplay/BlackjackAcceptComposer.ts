import { IMessageComposer } from '@nitrots/nitro-renderer';

export class BlackjackAcceptComposer implements IMessageComposer<number[]> {
    getMessageArray(): number[] {
        return [];
    }
    dispose(): void {
        /* no-op */
    }
}
