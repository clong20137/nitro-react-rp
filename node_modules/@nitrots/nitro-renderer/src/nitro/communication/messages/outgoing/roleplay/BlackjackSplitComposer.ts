import { IMessageComposer } from '@nitrots/nitro-renderer';

/** If you add multi-hand support, pass which hand to split. */
export class BlackjackSplitComposer implements IMessageComposer<number[]> {
    getMessageArray(): number[] {
        return [];
    }
    dispose(): void {
        /* no-op */
    }
}
