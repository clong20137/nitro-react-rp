import { IMessageEvent, MessageEvent } from '@nitrots/nitro-renderer/src/core';
import { GangMembersParser } from '@nitrots/nitro-renderer/src/nitro/communication/messages/parser/GangMembersParser';

export class GangMembersEvent extends MessageEvent implements IMessageEvent {
    constructor(callback: Function) {
        super(callback, GangMembersParser);
        console.log('Event data received for GangMembersEvent from Server');
    }

    public getParser(): GangMembersParser {
        return this.parser as GangMembersParser;
    }
}
