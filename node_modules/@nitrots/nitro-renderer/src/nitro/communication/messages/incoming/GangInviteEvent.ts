import { IMessageEvent, MessageEvent } from '@nitrots/nitro-renderer/src/core';
import { GangInviteParser } from '@nitrots/nitro-renderer/src/nitro/communication/messages/parser/GangInviteParser';

export class GangInviteEvent extends MessageEvent implements IMessageEvent {
    constructor(callback: Function) {
        super(callback, GangInviteParser);
                console.log("Event data recieved for GangInviteEvent from Server");
    }

    public getParser(): GangInviteParser {
        return this.parser as GangInviteParser;

    }
}
