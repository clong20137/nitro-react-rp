import { IMessageEvent, MessageEvent } from '@nitrots/nitro-renderer/src/core';
import { VirtualRoomInfoParser } from '@nitrots/nitro-renderer/src/nitro/communication/messages/parser/roleplay/VirtualRoomInfoParser';

export class VirtualRoomInfoEvent
    extends MessageEvent
    implements IMessageEvent
{
    constructor(callback: Function) {
        super(callback, VirtualRoomInfoParser);
        console.log('[🏷️] VirtualRoomInfoEvent initialized');
    }

    public getParser(): VirtualRoomInfoParser {
        return this.parser as VirtualRoomInfoParser;
    }
}
