import { IMessageEvent, MessageEvent } from '@nitrots/nitro-renderer';
import { NameIconEquippedParser } from '@nitrots/nitro-renderer/src/nitro/communication/messages/parser/roleplay/NameIconEquippedParser';

export class NameIconEquippedEvent extends MessageEvent implements IMessageEvent {
    constructor(callback: Function) {
        super(callback, NameIconEquippedParser);
    }

    public getParser(): NameIconEquippedParser {
        return this.parser as NameIconEquippedParser;
    }
}
