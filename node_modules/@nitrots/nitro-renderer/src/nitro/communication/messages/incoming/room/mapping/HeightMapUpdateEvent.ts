import { IMessageEvent } from '../../../../../../core/communication/messages/IMessageEvent';
import { MessageEvent } from '../../../../../../core/communication/messages/MessageEvent';
import { HeightMapUpdateParser } from '@nitrots/nitro-renderer/src/nitro/communication/messages/parser/room/mapping/HeightMapUpdateParser';

export class HeightMapUpdateEvent
    extends MessageEvent
    implements IMessageEvent
{
    constructor(callBack: Function) {
        super(callBack, HeightMapUpdateParser);
    }

    public getParser(): HeightMapUpdateParser {
        return this.parser as HeightMapUpdateParser;
    }
}
