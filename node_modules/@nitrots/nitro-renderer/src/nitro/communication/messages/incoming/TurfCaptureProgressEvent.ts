import { IMessageEvent, MessageEvent } from '@nitrots/nitro-renderer/src/core';
import { TurfCaptureProgressParser } from '@nitrots/nitro-renderer/src/nitro/communication/messages/parser/roleplay/TurfCaptureProgressParser';

export class TurfCaptureProgressEvent
    extends MessageEvent
    implements IMessageEvent
{
    constructor(callback: Function) {
        super(callback, TurfCaptureProgressParser);
        console.log(
            '📡 Event data received for TurfCaptureProgressEvent from Server'
        );
    }

    public getParser(): TurfCaptureProgressParser {
        return this.parser as TurfCaptureProgressParser;
    }
}
