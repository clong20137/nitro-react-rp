import { IMessageEvent, MessageEvent } from '@nitrots/nitro-renderer/src/core';
import { UserStatsParser } from '@nitrots/nitro-renderer/src/nitro/communication/messages/parser/roleplay/UserStatsParser';

export class UserStatsEvent extends MessageEvent implements IMessageEvent {
    constructor(callback: Function) {
        super(callback, UserStatsParser);
        console.log('[✅] Constructor from UserStatsEvent is loading!');
    }

    public getParser(): UserStatsParser {
        return this.parser as UserStatsParser;
    }
}