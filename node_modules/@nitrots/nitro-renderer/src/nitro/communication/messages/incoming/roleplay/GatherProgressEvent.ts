import { IMessageEvent, MessageEvent } from '@nitrots/nitro-renderer/src/core';
import { GatherProgressParser } from '@nitrots/nitro-renderer/src/nitro/communication/messages/parser/user/GatherProgressParser';
export class GatherProgressEvent extends MessageEvent implements IMessageEvent {
    constructor(callback: Function) {
        super(callback, GatherProgressParser);
    }

    public getParser(): GatherProgressParser {
        return this.parser as GatherProgressParser;
    }
}
