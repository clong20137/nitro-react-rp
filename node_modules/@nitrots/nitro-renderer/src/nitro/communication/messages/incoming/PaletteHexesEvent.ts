import { IMessageEvent, MessageEvent } from '@nitrots/nitro-renderer/src/core';
import { PaletteHexesParser } from '@nitrots/nitro-renderer/src/nitro/communication/messages/parser/PaletteHexesParser';

export class PaletteHexesEvent extends MessageEvent implements IMessageEvent {
    constructor(callback: Function) {
        super(callback, PaletteHexesParser);
        console.log('[🎨] PaletteHexesEvent initialized');
    }

    public getParser(): PaletteHexesParser {
        return this.parser as PaletteHexesParser;
    }
}
