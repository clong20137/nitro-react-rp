import { IMessageEvent, MessageEvent } from '@nitrots/nitro-renderer/src/core';
import { ChatBubbleShopParser } from '@nitrots/nitro-renderer/src/nitro/communication/messages/parser/ChatBubbleShopParser';

export class ChatBubbleShopEvent extends MessageEvent implements IMessageEvent {
    constructor(callback: Function) {
        super(callback, ChatBubbleShopParser);
         //console.log('[🗯] ChatBubbleShopEvent wired');
         console.log('[🚨] ChatBubbleShopEvent initialized!');
    }

    public getParser(): ChatBubbleShopParser {
        return this.parser as ChatBubbleShopParser;
    }
}
